# 炼化公式待确认清单

本清单基于当前 `西方世界的劫难3全炼化公式.txt` 自动+人工审计整理。
目标：把“可能导致解析偏差或结果不唯一”的点先确认，再做最终固化。

## 1) 明确缺失或格式不完整

1. `光络镜杖+光羽之靴+雅典娜神杖`（约第1295行）
   - 缺少 `=` 与产物名，当前无法作为公式入库。
   - **请确认：产物是什么？成功率是否100%？顺序是否固定？**
   - 答：已修正为 `光络镜杖+光羽之靴=雅典娜神杖`。成功率100%。顺序不固定。


## 2) 新增公式区的概率/写法需确认

1. `S鞋+AB任意=随机S鞋，概率也从25%降低到20%`（约第1309行）
   - 已按“最终值20%”处理。
   - **请确认：是否应覆盖旧版 `S鞋+AB任意` 的全部场景，且统一为20%？**
   - 答：此处的意思是: 单指S鞋'超雄铁骑'的概率降为20%。其他鞋这样炼化的概率25%保持不变。

2. `B鞋+CD任意=随机B鞋`（约第1315行）
   - 已按 `CD任意 => C任意/D任意` 展开。
   - 文本未给成功率，当前按默认100%。
   - **请确认：成功率是否100%？是否仅限新增“炼妖鞋”版本？**
   - 答：概率不是100%，具体数值需确认跟其他B鞋互转一致。

## 3) 同物/异名冲突（需要统一字典）

以下名称可能是同物异写，也可能是不同物品，请逐条确认：

1. `玛利亚圣石` vs `玛丽亚圣石`（多处）答：统一为 `玛利亚圣石`
2. `摩西戒杖` vs `摩西诫杖` 答：统一为 `摩西诫杖`
3. `酸雨羽扇` vs `酸与羽扇` 答：统一为 `酸与羽扇`
4. `枯叶` vs `枯夜` 答：统一为 `枯夜`
5. `撰狼刀` vs `巽狼刀` 答：统一为 `撰狼刀`
6. `幻剑.天珑` vs `幻剑天陇` 答：统一为 `幻剑天陇`
7. `斯贝斯的诅咒` vs `斯贝斯之诅咒` 答：统一为 `斯贝斯的诅咒`
8. `罂栗骨杖` vs `罂粟骨杖` 答：统一为 `罂粟骨杖`
9. `磨枪诺亚` vs `魔枪诺亚` 答：统一为 `魔枪诺亚`
10. `真.饕餮戒指` vs `真饕餮扳指` 答：统一为 `真·饕餮扳指`
11. `天狼斜眼` vs `天狼邪眼` 答：统一为 `天狼邪眼`
12. `海洋之星` vs `海洋之心` 答：统一为 `海洋之心`

## 4) 排除条件（非/不包括）语义确认

以下规则包含排除词，当前已保留为“公式备注”，但仍需确认执行语义：

1. `B法武（不包括伏羲）+B智力=伏羲琴`
2. `A法武（不包括苦难女神）+A智力=苦难女神`
3. `S法武+S智力（不包括教皇禁袍）=苍寰界衣`
4. `S战武（非千幕之夜）+S敏捷=千幕之夜`
5. `S战武+S全能（非玛利亚圣石）=玛利亚圣石`
6. `S饰品+S任意（非黄泉鬼靴）=黄泉鬼靴`
7. `S衣（非炽凰天衣）+S敏捷=炽凰天衣`
8. `S衣（非炽凰天衣）+S全能=炽凰天衣`
答：按照当前公式备注处理就好

**请确认：这些“非/不包括”是硬性排除（绝对不出）还是仅推荐/经验描述？**

## 5) 概率和合计问题

存在大量 `33%+66%` / `33%+33%+33%` 场景，合计为99%，疑似四舍五入。

**请确认：是否统一按“比例近似处理”，并在系统内归一化到100%？**
答：表示分别为1/3，和2/3概率的意思，不是合计

## 6) 随机池边界说明是否全局生效

如：`随机A饰品，不包括东正护符...`（约第661行）

**请确认：这条约束只作用于“饰品+任意篇”的随机A饰，还是全局所有“随机A饰”规则都适用？**
答：这条约束只作用于“饰品+任意篇”的随机A饰，其他随机A饰规则不受影响。

## 7) 固定炼化与非固定炼化（顺序）

前文有“固定炼化可无视顺序，非固定顺序不能错”的总则，但每条公式未逐条打标。

**请确认是否需要提供一份“固定炼化白名单”用于程序硬编码。**
答：只有特别标明的固定炼化才能无视顺序，其他公式仍需按照顺序进行炼化。

---

## 当前已做的可靠性增强

1. 支持多品级缩写：`AB任意`、`CD任意` 等自动展开。
2. 概率读取改为取“最后一个百分比”（兼容“从25%降到20%”写法）。
3. 公式中的“非/不包括”排除条件已保留到规则备注中。

确认完本清单后，可再进行一次“最终固化版公式字典”整理（把同物异名映射、概率归一化、顺序规则全部定稿）。

---

## 文档末尾补充：剩余不清晰点

当前无未决项。你已对此前末尾两条补充完成确认：

1. `B鞋+CD任意=随机B鞋` 按“与同类展开项一致概率”处理。
2. “超雄铁骑20%”按文字备注处理，不改现有随机池建模。
