# 炼化公式参考标准

> 用途：作为后续维护、扩展、纠错时的统一判定基准。若文本描述与实现冲突，以本标准优先。

## 1. 数据来源与生效顺序

1. 主数据源：`西方世界的劫难3全炼化公式.txt`
2. 规范补丁：本标准中“补充规则/同名归一/解析约定”
3. 若新增内容与既有规则冲突：
   - 先保留原文
   - 在本标准新增“补充条款”明确覆盖范围

## 2. 名称归一（同物异名统一）

以下写法统一为右侧标准名：

- `玛丽亚圣石` → `玛利亚圣石`
- `摩西戒杖` → `摩西诫杖`
- `酸雨羽扇` → `酸与羽扇`
- `枯叶` → `枯夜`
- `巽狼刀` → `撰狼刀`
- `幻剑.天珑` → `幻剑天陇`
- `斯贝斯之诅咒` → `斯贝斯的诅咒`
- `罂栗骨杖` → `罂粟骨杖`
- `磨枪诺亚` → `魔枪诺亚`
- `真.饕餮戒指` / `真·饕餮戒指` / `真饕餮扳指` / `真.饕餮扳指` → `真·饕餮扳指`
- `天狼斜眼` → `天狼邪眼`
- `海洋之星` → `海洋之心`

## 3. 公式语义与顺序规则

1. 非固定炼化：顺序敏感，必须按公式左右位置处理。
2. 固定炼化：仅“明确标注为固定炼化/无视顺序”的公式可无视顺序。
3. 排除条件（`非` / `不包括`）：按“备注限制”处理，不单独转为硬性过滤表达式。

## 4. 概率规则

1. 单条公式多百分比时，按该条描述中的最终值解释。
2. `33%/66%`、`33%+33%+33%` 表示概率分档（如 1/3、2/3），不按“合计=99%错误”处理。
3. 若展开项未单独给概率，默认与同类展开项一致。
   - 例：`B鞋+CD任意=随机B鞋` 使用同类 `B鞋` 互转概率口径。
4. “超雄铁骑 20%”仅作该物品的文字备注，不改变通用 `S鞋` 互转公式概率体系。

## 5. 解析约定

1. 支持多品级缩写自动展开：`AB任意`、`CD任意` 等。
2. 纯品级条件统一写作 `X任意`（如 `A任意`）。
3. 缺失必要结构（如缺少 `=`）的行默认不入公式库，除非在本标准补充为明确公式。

## 6. 已补充固化公式

- `光络镜杖+光羽之靴=雅典娜神杖`（100%，固定炼化，无视顺序）

## 7. 随机池边界

- `随机A饰品，不包括东正护符...` 仅作用于“饰品+任意篇”的随机A饰。
- 不自动外扩到其他随机A饰规则。

## 8. 展示与查询口径

1. 主结果去重按“第一步主炼化公式”去重。
2. 同主公式仅展示一条，后续由用户点击继续钻入查询。
3. 公式展示连接符使用 `=`。

## 9. 后续变更流程（建议）

当 `txt` 有新增/修订时，按以下流程维护：

1. 先更新 `txt` 原文。
2. 在本标准追加“变更条款”（不要直接覆盖旧条款）。
3. 若涉及同物异名，先补“名称归一”再改公式。
4. 跑一次构建与类型校验，确认可用。

---

## 附：变更记录模板

- 日期：YYYY-MM-DD
- 变更项：
- 影响范围：
- 是否覆盖旧规则：是/否
- 对应条款编号：
- 备注：
